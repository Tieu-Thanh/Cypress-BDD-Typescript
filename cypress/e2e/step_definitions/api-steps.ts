import { Given } from "@badeball/cypress-cucumber-preprocessor";
import APIClient from "../../support/api-client";

// var apiClient = new APIClient();

Given('my cart is empty', () => {
    // apiClient.sendRequest('GET', '/cart')
})
